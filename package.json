{
  "name": "listen-cx",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env dev --local",
    "dev:staging": "wrangler dev --env staging --local",
    "dev:staging:remote": "wrangler dev --env staging --remote",
    "setup:dev": "wrangler d1 create listen-cx-dev --env dev --location wnam --binding DB --update-config",
    "deploy:dev": "wrangler d1 migrations apply DB --env dev --remote && wrangler deploy --env dev",
    "deploy:staging": "wrangler d1 migrations apply DB --env staging --remote && wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env=\"\"",
    "migrate:local": "wrangler d1 migrations apply DB --env dev --local",
    "migrate:dev:remote": "wrangler d1 migrations apply DB --env dev --remote",
    "migrate:staging": "wrangler d1 migrations apply DB --env staging --remote",
    "migrate:staging:local": "wrangler d1 migrations apply DB --env staging --local",
    "migrate:production": "wrangler d1 migrations apply DB --remote",
    "types": "wrangler types",
    "types:check": "wrangler types --check",
    "check:startup": "wrangler check startup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "test:live": "vitest run --config vitest.live.config.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "hono": "^4.6.16",
    "nanoid": "^5.0.9",
    "zod": "^4.6.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.4",
    "@cloudflare/workers-types": "^5.20260713.1",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vite": "^8.1.4",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  }
}
